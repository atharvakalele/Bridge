# Install

See the README. Clone, then `./scripts/install.sh`, then restart Grok/Claude/Cline.

The live playbook is `skills/agy-cli/SKILL.md` (installed to `~/.grok/skills/agy-cli/SKILL.md`).
Do not use `agy-job`, `waiter.sh`, or `bridge-mcp-server` for new work.
