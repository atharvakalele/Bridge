#!/usr/bin/python3
"""Launch the live detached-job MCP (after install.sh copied agy_cli into ~/.config/bridge)."""
import os
import runpy

runpy.run_path(
    os.path.expanduser("~/.config/bridge/agy_cli/server.py"),
    run_name="__main__",
)
